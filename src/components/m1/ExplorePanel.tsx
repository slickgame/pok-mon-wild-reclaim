"use client";
import{useState}from'react';
import{encounterNames}from'@/data/m1/encounterNames';
export function ExplorePanel(){const[x,setX]=useState(''),[m,setM]=useState('Search the grove for movement.');const go=()=>{const n=encounterNames[Math.random()*encounterNames.length|0];setX(n);setM(`Wild ${n} appeared!`)};return <section><h2>Explore</h2><p>{m}</p><div style={{display:'flex',gap:8,flexWrap:'wrap'}}><button onClick={go}>Explore</button>{x&&<><button onClick={()=>setM(`Observed ${x}: calm but alert.`)}>Observe</button><button onClick={()=>{setX('');setM('You back away safely.')}}>Back Away</button><button onClick={()=>setM('Battle starts in M1.5.')}>Start Battle</button></>}</div></section>}